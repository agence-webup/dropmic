class Dropdown {

    constructor(target, options) {
        this.target = target;
        this.options = options;
    }

    setContent(content) {
        console.log(content);
    }

    addLink(label, link) {

    }

    addBtn(label, callback) {

    }
}
